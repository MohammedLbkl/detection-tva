git clone https://github.com/organisation/nom-du-projet.git
cd nom-du-projet

git clone

Mettre à jour son dépôt local :
git pull origin main

Créer une branche pour sa tâche :
git checkout -b feature/nom-fonctionnalité

Travailler, faire des commits clairs :
git add .
git commit -m "Ajout du module d’authentification"

Envoyer sa branche sur GitHub :
git push origin feature/nom-fonctionnalité

Merge Request : 
Tu vas sur GitLab, tu cliques sur "Merge Request", tu choisis ta branche et tu demandes à fusionner dans main.

Férification des branches:
git branch -a


Changer de branche :
git checkout nom_de_branche

voir ta branche : 
git status


# 1. Basculer sur la branche whisper
git checkout whisper

# 2. La mettre à jour avec le contenu exact de main
git reset --hard main

# 3. Pousser les changements pour écraser la branche distante
git push origin whisper --force







MyDocker:

cat resultats.txt
ls
rm -rf *
scp -r -P 10352 /Users/mohammedlbakali/Desktop/DTY/detection-notes/src/voice_assistant/test_MLB/cv_corpus_fr dazzling_saha@5.196.235.203:~/


venv:

python3 -m venv .venv
source .venv/bin/activate
deactivate



git checkout main
git pull origin main
git checkout Clean_Audio
git rebase main
git checkout main
git merge Clean_Audio
git push origin main

